# ESP32-DIV V2 (ESP32-S3-N16R8) - 16MB dual-OTA partition table
# Two 4MB app slots (ota_0/ota_1) so the SD-card / OTA firmware updater works.
# App image is ~1.7MB, so each slot has ~2.3MB headroom for growth (phase 2b, etc).
# Arduino IDE: a file named partitions.csv in the sketch folder overrides the
#   Tools > Partition Scheme menu automatically.
# PlatformIO: board_build.partitions = ESP32-DIV/partitions.csv
#
# Name,     Type, SubType,  Offset,    Size,      Flags
nvs,        data, nvs,      0x9000,    0x5000,
otadata,    data, ota,      0xE000,    0x2000,
app0,       app,  ota_0,    0x10000,   0x400000,
app1,       app,  ota_1,    0x410000,  0x400000,
spiffs,     data, spiffs,   0x810000,  0x7E0000,
coredump,   data, coredump, 0xFF0000,  0x10000,
